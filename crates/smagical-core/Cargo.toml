[package]
name = "smagical-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
toml = "0.9"
